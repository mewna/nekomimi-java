version: 2
jobs:
  build:
    machine: true
    steps:
    - checkout
    - run: docker login -u $DOCKER_USER -p $DOCKER_PASS
    - run: docker build -t mewna/nekomimi:$CIRCLE_BRANCH-$CIRCLE_BUILD_NUMBER .
    - run: docker push mewna/nekomimi:$CIRCLE_BRANCH-$CIRCLE_BUILD_NUMBER